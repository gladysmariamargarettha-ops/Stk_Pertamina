@page "/home"
@attribute [Microsoft.AspNetCore.Authorization.Authorize]

<PageTitle>Dokumen STK</PageTitle>

<h3>Dokumen STK</h3>

<p>
    Halaman ini adalah kerangka pengganti <code>home.php</code> (daftar dokumen STK dengan filter
    jenis/fungsi/status kadaluarsa memakai DataTables). Tabel dokumen lengkap beserta filter dan
    aksi Tambah/Edit/Hapus akan dibangun pada modul berikutnya
    (mengikuti tabel <code>dokumen</code> &amp; <code>fungsibaru</code>).
</p>
